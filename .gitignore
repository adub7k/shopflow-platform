# Tenant databases + customer-uploaded images live on the Railway volume in
# production — NEVER commit them (they contain password hashes and customer PII).
data/master.json
data/shops/
data/uploads/

# Local tooling / OS cruft
.claude/
.DS_Store
node_modules/

# Local secrets (Square/etc) — never commit
.env
.env.*
*.local.env
